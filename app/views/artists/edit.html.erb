<%= form_for(@artist) do |f| %>
    <%= f.label 'Artist Name' %><br>
    <%= f.text_field :name %><br>

    <%= f.label 'Artist Bio' %>
    <%= f.text_area :bio %>

    <%= f.submit "Update Artist"%>
<% end %>